name: Test

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: ls -a via ssh
      uses: stenfredd/psymfony@master
      with:
        host: 94.228.118.222
        username: root
        privateKey: ${{ secrets.SSH_KEY}}
        script:
          whoami